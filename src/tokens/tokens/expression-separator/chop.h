#ifndef CHOP_TOKEN_EXPRESSION_SEPARATOR_H
#define CHOP_TOKEN_EXPRESSION_SEPARATOR_H

#include <stdlib.h>
#include "source/source.h"

size_t chopExpressionSeparator(Source src, size_t offset);

#endif // CHOP_TOKEN_EXPRESSION_SEPARATOR_H
